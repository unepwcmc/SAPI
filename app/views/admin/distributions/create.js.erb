$('.modal').modal('hide');
$('.distributions-list').html("<%= escape_javascript(render('list')) %>");
window.adminEditor.initModals();
window.adminEditor.alertSuccess("Operation successful");
$("[rel='tooltip']").tooltip();
