<div class="admin-header">
  <h3>European Commission SRG</h3>
  <div class="action-buttons">
    <%= admin_add_new_button(
      :resource => 'ec_srg',
      :href => new_admin_ec_srg_url,
      :"data-toggle" => nil,
      :role => nil,
      :remote => true
    ) %>
  </div>
</div>

<%= admin_new_modal{ '' } %>
<%= admin_simple_search %>

<%= admin_table %>

<%= paginate collection %>