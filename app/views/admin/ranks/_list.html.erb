<thead>
  <th>Name</th>
  <th width="10%">Actions</th>
</thead>
<tbody>

  <% collection.each do |rank| -%>
    <tr>
      <td>
        <a href="#" class="editable editable-click editable-required" data-type="text" data-resource="rank" data-name="name"
          data-placeholder="Required" data-original-title="Enter the rank name"
          data-url="<%= resource_url(rank) %>" data-pk="<%= rank.id %>">
          <%= rank.name %>
        </a>
      </td>
      <td>
        <%= link_to 'Delete', resource_url(rank), :confirm => "Are you sure?", :method => :delete %>
      </td>
    </tr>
  <% end -%>
</tbody>