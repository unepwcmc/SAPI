<%= form_for :rank, :url => collection_url, :remote => true do |f| %>

  <%= error_messages_for(@rank) %>

  <label>Parent</label> <%= f.select :parent_id, options_from_collection_for_select(@ranks, :id, :name) %><br />
  <label>Name</label><%= f.text_field :name %><br />
<% end %>