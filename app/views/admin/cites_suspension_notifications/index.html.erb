<div class="admin-header">
  <h3>CITES Suspension Notifications</h3>
  <div class="action-buttons">
    <%= admin_add_new_button(
      :resource => 'cites_suspension_notification',
      :href => new_admin_cites_suspension_notification_url,
      :"data-toggle" => nil,
      :role => nil,
      :remote => true
    ) %>
  </div>
</div>

<%= admin_new_modal{ '' } %>
<%= admin_simple_search %>

<%= admin_table %>

<%= paginate collection %>
