<div class="admin-header">
  <h3>CITES PCs</h3>
  <div class="action-buttons">
    <%= admin_add_new_button(
      :resource => 'cites_pc',
      :href => new_admin_cites_pc_url,
      :"data-toggle" => nil,
      :role => nil,
      :remote => true
    ) %>
  </div>
</div>

<%= admin_new_modal{ '' } %>
<%= admin_simple_search %>

<%= admin_table %>

<%= paginate collection %>