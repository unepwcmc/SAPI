<h2>New merge: handle legislation</h2>
<%= merge_blurb %>
<p>
  <i class="icon-info-sign"></i> Legislation will be copied to all outputs of the merge. The following notes will be appended to copied objects.
</p>
<%= nomenclature_change_form do |f| %>
  <%= f.fields_for :input do |ff| %>
    <%= ff.fields_for :legislation_reassignments do |fff| %>
      <div class="control-group">
        <label class="control-label">
          <%= fff.object.reassignable_type %>
        </label>
        <div class="controls">
          <%= fff.text_area :note, {:rows => 4} %>
        </div>
      </div>
    <% end %>
  <% end %>
<% end %>
