<h2>New split: handle legislation</h2>
<%= split_blurb %>
<p>
  <i class="icon-info-sign"></i> Legislation will be copied to all outputs of the split. The following notes will be appended to copied objects.
</p>
<%= nomenclature_change_form do |f| %>
  <%= f.fields_for :input do |ff| %>
    <%= ff.fields_for :legislation_reassignments do |fff| %>
      <div class="control-group">
        <label class="control-label">
          <%= fff.object.reassignable_type %>
        </label>
        <div class="controls">
          <%= render partial: 'admin/nomenclature_changes/build/nomenclature_notes',
            locals: {ff: fff}
          %>
        </div>
      </div>
    <% end %>
  <% end %>
<% end %>