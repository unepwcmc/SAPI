<div class="admin-header">
  <h1>CITES Suspensions</h1>
  <div class="action-buttons">
    <%= admin_add_new_button(
      :resource => 'cites_suspension',
      :href => new_admin_cites_suspension_url,
      :"data-toggle" => nil,
      :role => nil
    ) %>
  </div>
</div>

<%= admin_simple_search %>
<%= admin_table %>

<%= paginate collection %>

