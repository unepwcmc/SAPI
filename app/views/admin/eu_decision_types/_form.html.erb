<%= form_for :eu_decision_type, :url => collection_url, :remote => true do |f| %>

  <%= error_messages_for(@eu_decision_type) %>

  <label>Name</label><%= f.text_field :name %><br />
  <label>Tooltip</label><%= f.text_field :tooltip %><br />
  <label>Is suspension?</label><%= f.check_box :is_suspension %>

<% end %>
