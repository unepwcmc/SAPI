{{#if multipleValues}}
  <div class='multiple-data-container'>
    <div class='document-data'>
      {{data.length}}&nbsp{{type}}
      <a class="tooltip">
        <i class='fa fa-info-circle'></i>
        <span>{{unbound formattedData}}
      </a>
    </div>
  </div>
{{else}}
  {{data}}
{{/if}}
